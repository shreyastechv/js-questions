function onClick() {
	const units = parseInt(document.getElementById("units").value);
	const output = document.getElementById("output");

	let copyUnits = units;
	let charge = 0;
	if (copyUnits >= 50) {
		
	}

	output.textContent = "";
}